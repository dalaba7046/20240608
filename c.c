#include <stdio.h>
#include <stdlib.h>


int main(){
    int n,m,a,b;
    scanf("%d %d",&n,&m);

    int input[n][m];
    int visit[n];

    for (int i=0;i<n;i++){
        visit[i] = i;
    }
    for(int i=0;i<a;++i){
        for(int j=0;j<b;++j){
            scanf("%d %d",&a,&b);
        }
    }

    // 如果景點[i]沒有走過,判斷是不是=1
    if (visit[i] == i){

    }
        // 如果走過景點[i] ,將1改成0
        visit[i] = 0

}